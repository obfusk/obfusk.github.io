---
title: Categories
order: 4

- dat = -> x { x.date }

%h1&= special.title
%hr
- extra.cats.sort.each do |c,ps|
  %h2&= c
  %ul
    - ps.sort_by(&dat).reverse.each do |p|
      %li
        &= p.date.strftime '%F %T'
        »
        %a{ href: p.link }&= p.title
